query profileAssets($address: String!) {
  Profile(where: { id: { _ilike: $address } }) {
    receivedAssets {
      asset {
        ...asset
      }
    }

    lsp12IssuedAssets {
      asset {
        ...asset
      }
    }

    holds {
      ...hold
    }
  }
}
