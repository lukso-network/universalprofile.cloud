query profileAssets($id: String!, $assets: [String!]) {
  Asset(where: { id: { _in: $assets } }) {
    ...asset
    tokens(where: { holders: { profile_id: { _eq: $id } } }) {
      tokenId
    }
  }
}
